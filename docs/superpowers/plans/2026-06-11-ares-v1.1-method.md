# Ares v1.1 "Método" — Plan

> Ejecutado inline por Claude (contexto completo cargado). Spec: ../specs/2026-06-11-ares-v1.1-method-design.md

**Datos verificados del SDK 0.3.173** (de sdk.d.ts, no re-investigar):
- `options.hooks: Partial<Record<HookEvent, HookCallbackMatcher[]>>`, con
  `HookCallbackMatcher = { matcher?: string; hooks: HookCallback[] }` y
  `HookCallback = (input, toolUseID, {signal}) => Promise<HookJSONOutput>`.
- Inyección de contexto en PostToolUse: devolver
  `{ hookSpecificOutput: { hookEventName: 'PostToolUse', additionalContext: '...' } }`.
- Thinking incremental: `stream_event` → `content_block_delta` → `delta.type === 'thinking_delta'`, texto en `delta.thinking`.
- TodoWrite: tool del preset claude_code; el tool_use lleva `input.todos: [{content, status, activeForm}]`.

## Tareas

1. **Alma**: `protocols/step-by-step.md` + `protocols/feedback.md`; soul.md los referencia (doctrina 7 y 8). Test de soul ampliado a 5 protocolos.
2. **core/agent.ts**: eventos `todos` y `thinking` en AgentEvent + emisión; hook PostToolUse (matcher `Write|Edit|MultiEdit|NotebookEdit`) que inyecta el recordatorio de verificación una vez por run.
3. **CLI** (`app.tsx`): checklist en vivo (☑ Laurel / ▸ Sky / ○ Steel) durante running; bloque de thinking atenuado (últimas 3 líneas, Sky, se limpia al llegar texto); headless ignora ambos.
4. **Telegram** (`telegram.ts`): caso TodoWrite en describeToolUse → "📋 n/m · paso en curso".
5. **Verificación**: typecheck + tests; integración tsx contra runAgent (opus, tarea multi-paso) asertando eventos todos/thinking; pty smoke del checklist; eval del protocolo feedback (corrección simulada → recuerdo type feedback).
