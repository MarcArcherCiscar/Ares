# Ares v1.4 "Craft" — Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) o superpowers:executing-plans. Pasos con checkbox (`- [ ]`). Usar `git -C /Users/marcarcher/Proyectos/Ares` siempre (el cwd del shell se resetea entre comandos).

**Goal:** Acercar a Ares a nivel de élite con tres disciplinas: TDD, ojos estructurales (CodeGraph MCP) y memoria arquitectónica por proyecto (`.ares/NOTES.md`).

**Architecture:** Una función pura de detección (`core/codegraph.ts`), dos integraciones en `core/agent.ts` (cargar `.ares/NOTES.md` + conectar el MCP de codegraph cuando el repo está indexado), y tres protocolos del alma (markdown).

**Tech Stack:** TypeScript (NodeNext, Node ≥20), Vitest 4, `@anthropic-ai/claude-agent-sdk` 0.3.x.

**Spec:** `docs/superpowers/specs/2026-06-13-ares-v1.4-craft-design.md`

**Datos verificados de CodeGraph 0.7.9 (no re-investigar):**
- Binario `codegraph` en PATH (`/opt/homebrew/bin/codegraph`).
- Servidor MCP stdio: `codegraph serve --mcp` (acepta `--path <repo>`).
- Estado: `codegraph status --json <path>` → JSON con campo `initialized` (bool). Exit code es 0 en ambos casos → **hay que parsear el JSON, no el exit code**. Si el binario no está, el spawn falla (ENOENT).
- Init: `codegraph init -i <path>` (`-i`=`--index`, NO interactivo).
- Señal de indexado canónica: `initialized: true` (el artefacto físico es `.codegraph/codegraph.db`).
- Config MCP para el SDK: `{ type: "stdio", command: "codegraph", args: ["serve", "--mcp", "--path", <cwd>] }`.

**Estructura de archivos:**

```
src/core/
├── codegraph.ts               # NUEVO — codegraphState(cwd, run?): "ready"|"needs-init"|"absent"
├── agent.ts                   # MODIFICAR — cargar .ares/NOTES.md + añadir MCP codegraph si ready
└── soul/
    ├── soul.md                # MODIFICAR — doctrina referencia los 3 protocolos
    └── protocols/
        ├── tdd.md             # NUEVO
        ├── structural-eyes.md # NUEVO
        └── project-notes.md   # NUEVO
tests/
└── codegraph.test.ts          # NUEVO — los 3 estados con runner inyectado
```

---

### Task 1: `codegraphState` — detección del estado de CodeGraph

**Files:**
- Create: `src/core/codegraph.ts`
- Test: `tests/codegraph.test.ts`

- [ ] **Step 1: Escribir el test que falla**

```ts
// tests/codegraph.test.ts
import { describe, it, expect } from "vitest";
import { codegraphState, type CodegraphRunner } from "../src/core/codegraph.js";

// runner falso: devuelve lo que le digamos, o null si simula binario ausente.
function runnerOf(result: { code: number; stdout: string } | null): CodegraphRunner {
  return () => result;
}

describe("codegraphState", () => {
  it("ready cuando status --json devuelve initialized:true", () => {
    const run = runnerOf({ code: 0, stdout: JSON.stringify({ initialized: true, fileCount: 10 }) });
    expect(codegraphState("/repo", run)).toBe("ready");
  });

  it("needs-init cuando status --json devuelve initialized:false", () => {
    const run = runnerOf({ code: 0, stdout: JSON.stringify({ initialized: false }) });
    expect(codegraphState("/repo", run)).toBe("needs-init");
  });

  it("absent cuando el binario no está (runner devuelve null)", () => {
    expect(codegraphState("/repo", runnerOf(null))).toBe("absent");
  });

  it("absent cuando la salida no es JSON parseable", () => {
    const run = runnerOf({ code: 0, stdout: "no soy json" });
    expect(codegraphState("/repo", run)).toBe("absent");
  });
});
```

- [ ] **Step 2: Verificar que falla**

Run: `cd /Users/marcarcher/Proyectos/Ares && npx vitest run tests/codegraph.test.ts`
Expected: FAIL — `Cannot find module '../src/core/codegraph.js'`

- [ ] **Step 3: Implementar**

```ts
// src/core/codegraph.ts
import { spawnSync } from "node:child_process";

export type CodegraphStateValue = "ready" | "needs-init" | "absent";

/** Ejecuta un comando codegraph; devuelve null si el binario no existe o el spawn falla. */
export type CodegraphRunner = (args: string[], cwd: string) => { code: number; stdout: string } | null;

/** Runner por defecto: lanza el binario `codegraph` de forma síncrona. */
const defaultRunner: CodegraphRunner = (args, cwd) => {
  try {
    const r = spawnSync("codegraph", args, { cwd, encoding: "utf8", timeout: 10_000 });
    if (r.error) return null; // ENOENT: binario no instalado
    return { code: r.status ?? 1, stdout: r.stdout ?? "" };
  } catch {
    return null;
  }
};

/**
 * Estado de CodeGraph para un repo:
 * - "ready": indexado (initialized:true) → se puede conectar el MCP.
 * - "needs-init": binario presente pero repo sin indexar → ofrecer init a Marc.
 * - "absent": sin binario o salida no fiable → grep/glob, sin mención.
 *
 * Parsea `codegraph status --json <cwd>`; el exit code es 0 en ambos casos, así
 * que la señal real es el campo `initialized` del JSON.
 */
export function codegraphState(cwd: string, run: CodegraphRunner = defaultRunner): CodegraphStateValue {
  const res = run(["status", "--json", cwd], cwd);
  if (!res) return "absent";
  try {
    const parsed = JSON.parse(res.stdout) as { initialized?: boolean };
    if (parsed.initialized === true) return "ready";
    if (parsed.initialized === false) return "needs-init";
    return "absent";
  } catch {
    return "absent";
  }
}
```

- [ ] **Step 4: Verificar que pasa**

Run: `cd /Users/marcarcher/Proyectos/Ares && npx vitest run tests/codegraph.test.ts`
Expected: PASS (4 tests). Luego `npm test` (toda la suite) y `npm run typecheck` limpios.

- [ ] **Step 5: Verificación real contra un repo indexado**

Run: `cd /Users/marcarcher/Proyectos/Ares && npx tsx -e "import('./src/core/codegraph.js').then(({codegraphState}) => { console.log('dafne-api:', codegraphState('/Users/marcarcher/Proyectos/FarmaVazquez/dafne-api')); console.log('Ares:', codegraphState(process.cwd())); })"`
Expected: imprime el estado real (dafne-api tiene `.codegraph/` → probablemente `ready`; Ares según esté indexado → `ready` o `needs-init`). Lo que se verifica es que el runner real funciona sin lanzar.

- [ ] **Step 6: Commit**

```bash
git -C /Users/marcarcher/Proyectos/Ares add src/core/codegraph.ts tests/codegraph.test.ts
git -C /Users/marcarcher/Proyectos/Ares commit -m "feat(core): codegraphState — detección de estado de CodeGraph (ready/needs-init/absent)"
```

---

### Task 2: Integrar en `core/agent.ts` — NOTES.md + MCP de codegraph

**Files:**
- Modify: `src/core/agent.ts`

Leer el archivo completo primero para ubicar (a) la construcción del array `append` (que hoy incluye `loadSoul()`, el índice de memoria, channelInstructions, projectInstructions) y (b) el objeto `mcpServers` dentro de `query({ options: {...} })` (hoy `mcpServers: { ares: buildToolbelt({ outputDir: opts.outputDir }) }`).

- [ ] **Step 1: Importar las dependencias nuevas**

Al principio de `src/core/agent.ts`, junto a los imports existentes, añadir:

```ts
import { readFileSync, existsSync } from "node:fs";
import { join } from "node:path";
import { codegraphState } from "./codegraph.js";
```

(Si `readFileSync`/`existsSync`/`join` ya están importados de antes, no duplicar — reutilizar.)

- [ ] **Step 2: Cargar `.ares/NOTES.md` en el system prompt**

En la construcción del array `append` (dentro de `runAgent`), justo después del elemento de la memoria global y antes de `channelInstructions`, insertar un elemento nuevo. El array pasa a incluir:

```ts
    // Notas de arquitectura del proyecto, si las hay (.ares/NOTES.md del repo).
    (() => {
      const notesPath = join(opts.cwd, ".ares", "NOTES.md");
      if (!existsSync(notesPath)) return "";
      try {
        const notes = readFileSync(notesPath, "utf8").trim();
        return notes ? `# Notas del proyecto (.ares/NOTES.md)\n\n${notes}` : "";
      } catch {
        return "";
      }
    })(),
```

(Va como un elemento más del array que luego se `.filter(Boolean).join("\n\n---\n\n")`. Mantener el orden: alma → memoria global → notas del proyecto → canal → instrucciones de proyecto.)

- [ ] **Step 3: Conectar el MCP de codegraph cuando el repo está indexado**

Antes de la llamada a `query(...)`, calcular el estado y construir el bloque de mcpServers:

```ts
  // Ojos estructurales: si el repo está indexado, conecta el MCP de CodeGraph.
  const cgState = codegraphState(opts.cwd);
  const codegraphServer =
    cgState === "ready"
      ? { codegraph: { type: "stdio" as const, command: "codegraph", args: ["serve", "--mcp", "--path", opts.cwd] } }
      : {};
```

Y en las `options` de `query`, cambiar la línea de `mcpServers` para fusionar ambos:

```ts
      mcpServers: { ares: buildToolbelt({ outputDir: opts.outputDir }), ...codegraphServer },
```

Nota: el estado `needs-init` NO se actúa aquí (no se conecta MCP ni se inicializa); el protocolo `structural-eyes.md` (Task 3) le dice a Ares que ofrezca el init a Marc. `codegraphState` ya degrada a `absent` si el binario no existe, así que en máquinas sin codegraph esto es `{}` y no cambia nada.

- [ ] **Step 4: Typecheck + tests**

Run: `cd /Users/marcarcher/Proyectos/Ares && npm run typecheck && npm test`
Expected: typecheck limpio (si el tipo de `mcpServers` se queja del server stdio, comprobar en `node_modules/@anthropic-ai/claude-agent-sdk/sdk.d.ts` la forma exacta de `McpStdioServerConfig` y ajustar las claves — la doc indica `type: "stdio"`, `command`, `args`); tests siguen pasando.

- [ ] **Step 5: Verificación real (NOTES.md se inyecta)**

```bash
cd /Users/marcarcher/Proyectos/Ares && mkdir -p /tmp/ares-notes-test/.ares && echo "GOTCHA: el puerto 5123 lo usa otro servicio, usar 5124." > /tmp/ares-notes-test/.ares/NOTES.md
npx tsx src/cli/index.ts -p "¿Qué sabes de este proyecto por sus notas? Responde en una línea." -m haiku
```
Expected (ejecutar desde `/tmp/ares-notes-test` no hace falta; el `-p` usa cwd actual = repo Ares, que NO tiene NOTES.md). Para probar de verdad la carga, ejecutar el `-p` CON cwd en el repo de prueba:
```bash
cd /tmp/ares-notes-test && ares -p "¿Qué sabes de este proyecto por sus notas? Responde en una línea." -m haiku
```
Expected: la respuesta menciona el gotcha del puerto 5123/5124 → confirma que `.ares/NOTES.md` se cargó en el system prompt. (Requiere `npm run build` + el binario linkeado; si no, usar `npx tsx <ruta>/src/cli/index.ts`.)

- [ ] **Step 6: Commit**

```bash
git -C /Users/marcarcher/Proyectos/Ares add src/core/agent.ts
git -C /Users/marcarcher/Proyectos/Ares commit -m "feat(core): cargar .ares/NOTES.md + conectar MCP de codegraph cuando el repo está indexado"
```

---

### Task 3: Los tres protocolos del alma

**Files:**
- Create: `src/core/soul/protocols/tdd.md`
- Create: `src/core/soul/protocols/structural-eyes.md`
- Create: `src/core/soul/protocols/project-notes.md`
- Modify: `src/core/soul/soul.md`
- Modify: `tests/soul.test.ts`

- [ ] **Step 1: Escribir `tdd.md`** (contenido completo)

```markdown
# Protocolo: tests primero (TDD)

Cuándo aplica: al implementar lógica nueva no trivial o arreglar un bug. NO
aplica a config, estilos CSS, prototipos visuales de UI, docs ni cambios
triviales — ahí los tests aportan poco; si saltas tests, di por qué en una línea.

## Feature (lógica nueva)
1. Escribe el test que describe el comportamiento deseado.
2. Ejecútalo y **velo fallar** (rojo). Si pasa a la primera, el test no prueba
   nada nuevo — arréglalo.
3. Implementa lo mínimo para que pase (verde).
4. Refactoriza con el test de red.
No te saltes el paso 2: un test que nunca viste fallar no es una prueba, es una
ilusión.

## Bug
1. Escribe primero un test que **reproduce** el fallo (rojo).
2. Arregla la causa (verde).
3. El test se queda como regresión.

El test es la especificación ejecutable. Si no sabes qué test escribir, es que
aún no entiendes bien qué tienes que construir — vuelve a la spec.
```

- [ ] **Step 2: Escribir `structural-eyes.md`** (contenido completo)

```markdown
# Protocolo: ojos estructurales (CodeGraph)

Cuándo aplica: cuando necesitas entender la ESTRUCTURA del código —dónde se
define algo, quién llama a qué, qué se rompería si cambias X— no solo buscar
texto.

1. **Si tienes las tools `codegraph_*`** (el repo está indexado y el MCP
   conectado), úsalas para preguntas estructurales:
   - "¿dónde está X?" → `codegraph_search`
   - "¿quién llama a Y?" → `codegraph_callers`
   - "¿qué rompe cambiar Z?" → `codegraph_impact`
   - contexto de un área → `codegraph_context`
   Son sub-ms y vienen de un AST real: fíate de ellas y NO las reverifiques con
   grep. Reserva grep/glob para texto literal (strings, comentarios, logs).
2. **Si NO tienes esas tools pero CodeGraph está instalado y el repo sin
   indexar**: ofrece a Marc inicializarlo —"¿indexo el repo con codegraph para
   tener búsqueda estructural? (`codegraph init -i`)"— UNA vez por sesión. Si
   dice que no, sigue con grep/glob sin insistir.
3. **Si CodeGraph no está**: grep/glob como siempre, sin mencionar nada.
```

- [ ] **Step 3: Escribir `project-notes.md`** (contenido completo)

```markdown
# Protocolo: notas del proyecto (.ares/NOTES.md)

Cuándo aplica: cuando descubres algo no obvio sobre un repo que tu yo futuro
agradecería saber al volver. Las notas viven en `<repo>/.ares/NOTES.md` y se
cargan al arrancar Ares en ese repo.

## Qué anotar
- Gotchas: "esto parece roto pero es a propósito porque…".
- Decisiones de arquitectura y su porqué (lo que no se ve en el código).
- Campos minados: "no toques X sin Y", dependencias frágiles, orden que importa.
- Comandos no obvios para correr/probar/desplegar este repo.

## Qué NO anotar
- Lo que ya está en el código, el README, el CLAUDE.md o git.
- Estado efímero de la conversación de hoy.
- Cosas de Marc (preferencias personales) — eso va a la memoria global con
  `remember`, no aquí.

## Cómo
Edita `<repo>/.ares/NOTES.md` con tus tools de archivo (créalo si no existe).
Una entrada corta por hallazgo, con el porqué. No lo conviertas en un diario:
solo lo que de verdad ahorra tiempo la próxima vez.
```

- [ ] **Step 4: Referenciar en `soul.md`**

Leer `src/core/soul/soul.md`; tras el punto 9 de la doctrina (el del flujo), añadir los puntos 10-12:

```markdown
10. **Tests primero.** En lógica nueva no trivial y en bugs, el test va antes
    que el código (velo fallar). Sigue `protocols/tdd.md`.
11. **Usa los ojos estructurales.** Para entender qué llama a qué o qué rompe un
    cambio, usa las tools `codegraph_*` si están; si no, grep. Sigue
    `protocols/structural-eyes.md`.
12. **Anota lo no obvio del repo.** Cuando descubras un gotcha o una decisión que
    tu yo futuro agradecería, guárdalo en `.ares/NOTES.md`. Sigue
    `protocols/project-notes.md`.
```

- [ ] **Step 5: Ampliar el test del alma**

En `tests/soul.test.ts`, añadir las tres marcas nuevas al array y actualizar la descripción del `it(...)` a `"incluye identidad, los once protocolos y el bloque de carácter"`. El array completo:

```ts
    for (const marca of [
      "Protocolo: debugging",
      "buscar antes de crear",
      "verificación antes de afirmar",
      "trabajo por pasos",
      "aprender de las correcciones",
      "discrepar bien",
      "flujo disciplinado",
      "investigar antes de especificar",
      "tests primero",
      "ojos estructurales",
      "notas del proyecto",
    ]) {
      expect(soul).toContain(marca);
    }
```

- [ ] **Step 6: Verificar**

Run: `cd /Users/marcarcher/Proyectos/Ares && npm test && npm run typecheck`
Expected: tests pasan (soul comprueba 11 protocolos), typecheck limpio.

- [ ] **Step 7: Commit**

```bash
git -C /Users/marcarcher/Proyectos/Ares add src/core/soul/ tests/soul.test.ts
git -C /Users/marcarcher/Proyectos/Ares commit -m "feat(soul): protocolos tdd + structural-eyes + project-notes; doctrina de craft"
```

---

### Task 4: Verificación final + README + cierre

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Suite + build**

Run: `cd /Users/marcarcher/Proyectos/Ares && npm test && npm run typecheck && npm run build`
Expected: todos los tests pasan (suite previa + 4 de codegraph), typecheck limpio, build OK (verifica que los protocolos nuevos se copian a `dist/core/soul/protocols/`).

- [ ] **Step 2: Verificación de integración de ojos estructurales (si hay repo indexado)**

```bash
cd /Users/marcarcher/Proyectos/FarmaVazquez/dafne-api && ares -p "Usa codegraph si lo tienes: ¿cuántos símbolos/archivos ve el índice de este repo? Una línea." -m haiku
```
Expected: si dafne-api está indexado, Ares usa una tool `codegraph_*` y responde con datos del índice → confirma que el MCP se conectó. Si no está indexado, Ares ofrece inicializarlo (comportamiento `needs-init` correcto). READ la salida y reporta cuál de los dos casos ocurrió.

- [ ] **Step 3: Escaneo de seguridad**

Ejecutar `snyk_code_scan` sobre el repo. Arreglar issues introducidos por el código nuevo (`codegraph.ts`, cambios en `agent.ts`) y re-escanear. Nota: spawnSync de un binario fijo (`codegraph`) con args controlados no debería disparar nada; si Snyk marca command injection por el cwd, confirmar que `cwd` viene del propio proceso (no de input externo) y documentarlo.

- [ ] **Step 4: README**

En `README.md`, en la sección de arquitectura, tras el bullet del flujo disciplinado, añadir:

```markdown
- **Craft**: Ares aplica TDD en lógica nueva (test primero,
  `soul/protocols/tdd.md`), usa los ojos estructurales de CodeGraph cuando el
  repo está indexado (`core/codegraph.ts` conecta su MCP; si no, grep), y
  mantiene notas de arquitectura por repo en `<repo>/.ares/NOTES.md`
  (autoescritas, cargadas al arrancar).
```

- [ ] **Step 5: Commit + push**

```bash
git -C /Users/marcarcher/Proyectos/Ares add README.md
git -C /Users/marcarcher/Proyectos/Ares commit -m "docs: README — disciplina de craft (TDD, ojos estructurales, notas por proyecto)"
git -C /Users/marcarcher/Proyectos/Ares push origin main
```

- [ ] **Step 6: Cierre**

Usar `superpowers:finishing-a-development-branch` (estamos en `main`, push directo ya hecho — confirmar working tree limpio y sincronía con origin).

---

## Cobertura de la spec (self-check)

| Requisito de la spec | Task |
|---|---|
| TDD estricto en features, test-first en bugs, flexible en no-testeable | 3 (tdd.md) |
| `codegraphState` ready/needs-init/absent | 1 |
| Detección por `status --json` (parsear initialized) | 1 |
| Conectar MCP codegraph cuando ready | 2 |
| needs-init → ofrecer init, no actuar solo | 2 (no conecta) + 3 (structural-eyes.md ofrece) |
| absent → grep, sin ruido | 1 (degrada) + 3 |
| `.ares/NOTES.md` autoescrita | 3 (project-notes.md) |
| `.ares/NOTES.md` cargada al arrancar | 2 |
| Protocolo de qué anotar / qué no | 3 |
| Manejo de errores (MCP falla, NOTES ilegible) | 1, 2 (try/catch) |
| Tests de los 3 estados | 1 |
| Doctrina referencia los 3 protocolos | 3 (soul.md) |
